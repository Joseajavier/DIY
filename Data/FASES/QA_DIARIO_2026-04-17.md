# Informe QA Diario — 2026-04-17

**Fecha/Hora:** 2026-04-17 05:00 Madrid (agente QA automatizado)
**Estado General:** ⚠️ WARN — regresión TS por Features Builder (+233 errores), sin bugs críticos de runtime
**Comparativa:** Feature Builder añadió nuevas pantallas → 173 → 421. Bug Fixer redujo 421 → 406. Delta neto vs ayer: **+233 (+134.7%) PEOR**

---

## 1. Commits Nocturnos Revisados

| SHA | Mensaje | Impacto |
|-----|---------|---------|
| `1790886` | feat: avance feature #2 (WoodCatalogScreen error) | ErrorState + retry handler → 407→406 |
| `00ebd20` | fix(D0): aplicar 3 fixes de tipado del QA (2026-04-17) | -15 errores (Chip key + DIYStepsScreen + ToolSearchScreen) |
| `67e316d` | feat(catalog): +5 tools +3 wood (adaptive 2026-04-17) | tools.json 107→112, wood.json 28→31 |
| `67a2937` | fix(pdf): lazy load expo-print para evitar crash en Expo Go | require lazy en exportParametricPdf |
| `ea3a563` | chore(parametric): completar wiring de Fase 6 (export PDF) | wiring exportParametricPdf en generadores |
| `cebe57b` | feat(parametric): lista automática de herrajes por plantilla (Fase 22.2) | herrajes por plantilla |
| `837af00` | feat(parametric): añadir exportar PDF del despiece (Fase 6) | exportParametricPdf.ts nuevo |
| `c13e60b` | feat(parametric): preview 3D con puertas y cajones reales | 3D mejorado |
| `bec9139` | feat(parametric): guardar muebles paramétricos como proyecto (Fase 5) | integración DB |
| `7336de7` | feat(parametric): 3 plantillas nuevas — cajonera, armario, banco (Fase 4) | +3 pantallas |

**Causa de regresión:** Los commits `7336de7` → `ea3a563` añadieron ~8 nuevas pantallas paramétricas + CompareSheet nuevo + expansión de ProjectDetailScreen y BoardFootCalcScreen. Cada archivo nuevo trae sus errores de infraestructura (TS2307 por `react`/`react-native` no instalados) y cascada de TS7006 (implicit any).

---

## 2. TypeScript — Frontend (raíz)

**Total errores HOY:** 406
**Total errores AYER:** 173
**Delta: +233 errores (+134.7%) ⚠️ PEOR**

| Código | Descripción | Ayer | Hoy | Delta |
|--------|-------------|------|-----|-------|
| TS2307 | Cannot find module (react, expo, etc.) | 111 | 210 | **+99** ⬆️ |
| TS7006 | Implicit any parameter | 41 | 114 | **+73** ⬆️ |
| TS2591 | `process`/`require` no declarados | 4 | 36 | **+32** ⬆️ |
| TS2322 | Type mismatch | 9 | 12 | +3 ⬆️ |
| TS2741 | Missing required property | 0 | 8 | **+8 NUEVO** |
| TS2304 | Global no declarado (`__DEV__`) | 8 | 8 | = |
| TS2745 | JSX children type mismatch | 0 | 7 | **+7 NUEVO** |
| TS7031 | Binding element implicit any | ? | 6 | ⬆️ |
| TS7053 | Implicit any indexing | 0 | 4 | **+4 NUEVO** |
| TS6053 | File not found | 0 | 1 | +1 |

### Archivos con más errores
| Archivo | Errores |
|---------|---------|
| `src/assets/brands/index.ts` | 27 |
| `src/components/CompareSheet.tsx` | 24 |
| `src/screens/ProjectDetailScreen.tsx` | 21 |
| `src/screens/calculators/BoardFootCalcScreen.tsx` | 20 |
| `src/screens/DIYInputScreen.tsx` | 15 |
| `src/screens/ProInputScreen.tsx` | 12 |
| Pantallas paramétricas (×8 archivos) | 68 |

### Clasificación
- **Infraestructura** (resuelven con `npm install`): ~320 (TS2307 + cascadas TS7006/TS7031 + TS2304)
- **Lógicos reales** (persisten sin npm): ~86 (TS2591×36, TS2322×12, TS2741×8, TS2745×7, TS7053×4)

---

## 3. TypeScript — Backend (`backend/`)

**Total errores HOY:** 1
**Total errores AYER:** 1
**Delta: 0 (IGUAL)**

| Código | Archivo | Descripción |
|--------|---------|-------------|
| TS2688 | `backend/tsconfig.json` | Cannot find type definition for `node` — `@types/node` no instalado |

---

## 4. Validación JSONs de Catálogos

| Archivo | Estado | Detalles |
|---------|--------|----------|
| `backend/data/tools.json` | ✅ VALID | 112 productos, 10 categorías, 40 marcas — +5 vs ayer (107→112) |
| `backend/data/wood.json` | ✅ VALID | 31 productos — +3 vs ayer (28→31) |

> **Nota:** `tools.json` ya no tiene campo `totalProducts` en el JSON raíz (se eliminó o se cuenta desde `products.length`). Los datos son correctos; el campo era redundante.

---

## 5. Botones Rotos / Funcionalidad Incompleta

### Patrones de botones rotos
| Patrón | Resultado |
|--------|-----------|
| `onPress={() => {}}` vacíos | ✅ NINGUNO |
| `throw new Error('not implemented')` | ✅ NINGUNO |
| `TODO` / `FIXME` nuevos en `src/` | ⚠️ 4 pre-existentes en `src/data/retailers.ts` |

### TODOs detectados (pre-existentes, no nuevos esta noche)
```
src/data/retailers.ts:87   // TODO: añadir tag cuando se registre afiliado en amazon.de
src/data/retailers.ts:126  // TODO: añadir tag cuando se registre afiliado en amazon.fr
src/data/retailers.ts:165  // TODO: añadir tag cuando se registre afiliado en amazon.co.uk
src/data/retailers.ts:204  // TODO: añadir tag cuando se registre afiliado en amazon.it
```
Estos son TODOs de negocio (registrar como afiliado de Amazon), no bugs técnicos.

---

## 6. TOP 5 BUGS — Para Bug Fixer 2026-04-18

### 🔴 #1 — `src/components/CompareSheet.tsx:364-377` — TS2741 × 8: `children` requerido no reconocido en JSX

**Descripción:** `ValueCell` declara `children: React.ReactNode` como propiedad requerida. Sin `@types/react` instalados, el compilador TSX no injeta automáticamente los `children` JSX en el tipo inferido de props → ve `{ diff: any }` sin `children` → TS2741 en 8 usos (líneas 238, 244, 250, 256, 273, 286, 292, 301).

**Código ACTUAL (líneas 364-377):**
```tsx
function ValueCell({
  children,
  diff,
  alt,
  tall,
}: {
  children: React.ReactNode;
  diff: boolean;
  alt?: boolean;
  tall?: boolean;
}) {
```

**Código DESPUÉS (fix):**
```tsx
function ValueCell({
  children,
  diff,
  alt,
  tall,
}: {
  children?: React.ReactNode;
  diff: boolean;
  alt?: boolean;
  tall?: boolean;
}) {
```
Hacer `children` opcional (`children?: React.ReactNode`) elimina los 8 TS2741. El valor en runtime nunca es undefined porque los JSX siempre pasan contenido, pero TypeScript sin React types no puede inferirlo.

**Archivo:** `src/components/CompareSheet.tsx:370`
**Errores eliminados:** 8 × TS2741

---

### 🔴 #2 — `src/screens/DIYInputScreen.tsx:66` y `src/screens/ProInputScreen.tsx` — TS2745 × 7+: `SectionTitle children: string` falla con texto

**Descripción:** `SectionTitle` está definido como `function SectionTitle({ children }: { children: string })`. Sin `@types/react`, TypeScript interpreta `children: string` como que el componente "requiere múltiples children" y cuando se usa `<SectionTitle>Texto aquí</SectionTitle>` con un solo string child → TS2745 en 7 líneas de DIYInputScreen y equivalentes en ProInputScreen.

**Código ACTUAL (DIYInputScreen.tsx:66):**
```tsx
function SectionTitle({ children }: { children: string }) {
  return <Text style={styles.sectionTitle}>{children}</Text>;
}
```

**Código DESPUÉS (fix):**
```tsx
function SectionTitle({ children }: { children: React.ReactNode }) {
  return <Text style={styles.sectionTitle}>{children}</Text>;
}
```
Cambiar `string` → `React.ReactNode` elimina los 7 TS2745 en DIYInputScreen. Aplicar el mismo cambio en ProInputScreen si tiene un `SectionTitle` idéntico.

**Archivos:** `src/screens/DIYInputScreen.tsx:66`, `src/screens/ProInputScreen.tsx` (buscar definición análoga)
**Errores eliminados:** 7 × TS2745 (o más si ProInputScreen tiene el mismo patrón)

---

### 🔴 #3 — `src/screens/ProjectDetailScreen.tsx:52-95` — TS7006 × 13: callbacks setState con `prev`/`s`/`p` implícitamente `any`

**Descripción:** Todos los callbacks de `setSteps()` y `setProject()` usan parámetros sin tipo explícito. Sin `react` instalado, `setState` devuelve `Dispatch<SetStateAction<any>>` → los parámetros de los callbacks se vuelven `any` implícito → TS7006 en 13 lugares.

**Código ACTUAL (líneas 52-63):**
```tsx
setSteps((prev) =>
  prev.map((s) => (s.id === step.id ? { ...s, completed: !s.completed } : s)),
);
// ...
setSteps((prev) =>
  prev.map((s) => (s.id === step.id ? { ...s, completed: !s.completed } : s)),
);
```

**Código DESPUÉS (fix):**
```tsx
setSteps((prev: ProjectStep[]) =>
  prev.map((s: ProjectStep) => (s.id === step.id ? { ...s, completed: !s.completed } : s)),
);
// ...
setSteps((prev: ProjectStep[]) =>
  prev.map((s: ProjectStep) => (s.id === step.id ? { ...s, completed: !s.completed } : s)),
);
```
Aplicar `prev: ProjectStep[]` y `s: ProjectStep` en todos los callbacks de `setSteps`, y `p: Project` en `setProject`. Igualmente `p: Piece`, `idx: number` en líneas 71, 92-95, 174-176, 215, 234, 246.

**Archivo:** `src/screens/ProjectDetailScreen.tsx:52,53,62,63,71,92,94,95,174,176,215,234,246`
**Errores eliminados:** 13 × TS7006

---

### 🟡 #4 — `src/services/parametric/exportParametricPdf.ts:323,325` — TS2591 × 2+: `require` no declarado en lazy imports

**Descripción:** El servicio usa `require()` para lazy-load `expo-print` y `expo-sharing` (patrón correcto para Expo Go). Sin `@types/node` en tsconfig, `require` no existe como global → TS2591. Los comentarios `eslint-disable` están presentes pero no silencian TypeScript.

**Código ACTUAL (líneas 323-325):**
```tsx
// eslint-disable-next-line @typescript-eslint/no-require-imports
const Print = require('expo-print') as typeof import('expo-print');
// eslint-disable-next-line @typescript-eslint/no-require-imports
const Sharing = require('expo-sharing') as typeof import('expo-sharing');
```

**Código DESPUÉS (fix):**
```tsx
// eslint-disable-next-line @typescript-eslint/no-require-imports
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const Print = (globalThis as any).require('expo-print') as typeof import('expo-print');
// eslint-disable-next-line @typescript-eslint/no-require-imports
// eslint-disable-next-line @typescript-eslint/no-explicit-any
const Sharing = (globalThis as any).require('expo-sharing') as typeof import('expo-sharing');
```

O alternativamente, declarar `require` al inicio del archivo (más limpio):
```tsx
// Al inicio del archivo, después de los imports:
declare function require(module: string): any;
```

**Archivo:** `src/services/parametric/exportParametricPdf.ts:323,325`
**Errores eliminados:** 2 × TS2591 + 2 × TS2307 (expo-print / expo-sharing cascada)

---

### 🟡 #5 — Pantallas paramétricas (×6 archivos) — TS7006 × ~48: callbacks `.map()` sin tipos explícitos

**Descripción:** Los 6 generadores paramétricos (`TableGeneratorScreen`, `CabinetGeneratorScreen`, `BenchGeneratorScreen`, `ShelfGeneratorScreen`, `DrawerCabinetGeneratorScreen`, `BoxGeneratorScreen`) tienen el mismo patrón: callbacks de arrays con parámetros `(w, i)`, `(p, i)`, `(n, i)` implícitamente `any`. Cada archivo tiene ~8-11 errores de este tipo.

**Código ACTUAL (TableGeneratorScreen.tsx:183, patrón repetido en todos):**
```tsx
{config.woods.map((w, i) => (       // w: any, i: any → TS7006×2
  <WoodOption key={i} item={w} ... />
))}
{pieces.map((p, i) => (             // p: any, i: any → TS7006×2
  <PieceRow key={i} piece={p} ... />
))}
{hardware.map((n, i) => (           // n: any, i: any → TS7006×2
  <HardwareRow key={i} item={n} ... />
))}
```

**Código DESPUÉS (fix, patrón para aplicar en los 6 archivos):**
```tsx
// Importar los tipos necesarios al inicio (WoodOption, Piece, HardwareItem, etc.)
import type { WoodOption, Piece, HardwareItem } from '../../models/parametric';

{config.woods.map((w: WoodOption, i: number) => (
  <WoodOption key={i} item={w} ... />
))}
{pieces.map((p: Piece, i: number) => (
  <PieceRow key={i} piece={p} ... />
))}
{hardware.map((n: HardwareItem, i: number) => (
  <HardwareRow key={i} item={n} ... />
))}
```
El tipo exacto de `w`, `p`, `n` depende de los modelos declarados en cada generador. Si los modelos están en `src/models/parametric.ts`, importarlos. Si son interfaces locales del archivo, referenciarlas directamente.

**Archivos:** `src/screens/parametric/TableGeneratorScreen.tsx:183,204,226,251`, `CabinetGeneratorScreen.tsx`, `BenchGeneratorScreen.tsx`, `ShelfGeneratorScreen.tsx`, `DrawerCabinetGeneratorScreen.tsx`, `BoxGeneratorScreen.tsx`
**Errores eliminados:** ~48 × TS7006 (~8 por archivo × 6 archivos)

---

## 7. Resumen Ejecutivo

| Categoría | Ayer (2026-04-16) | Hoy (2026-04-17) | Delta |
|-----------|-------------------|-------------------|-------|
| **Estado general** | ✅ OK | ⚠️ WARN | PEOR |
| **Errores TS frontend (total)** | 173 | 406 | **+233 (+134.7%)** |
| **Errores TS backend** | 1 | 1 | = |
| Errores lógicos reales frontend | ~25 | ~86 | +61 |
| TS2741 (missing children) | 0 | 8 | **+8 NUEVO** |
| TS2745 (JSX children type) | 0 | 7 | **+7 NUEVO** |
| TS7053 (implicit any indexing) | 0 | 4 | +4 |
| tools.json | ✅ OK (107 prods) | ✅ OK (112 prods) | +5 prods |
| wood.json | ✅ OK (28 prods) | ✅ OK (31 prods) | +3 prods |
| Botones vacíos | 0 | 0 | = |
| TODOs nuevos en código | 0 | 0 | = |
| Pantallas paramétricas nuevas | 0 | +8 pantallas | NUEVO |
| CompareSheet.tsx | no existía | 24 errores | NUEVO |

### Causa raíz de la regresión
La regresión **NO es por rotura del código existente** sino por adición de código nuevo (Feature Builder 2026-04-17) sin tipos explícitos. Las 8 pantallas paramétricas, CompareSheet, y la expansión de ProjectDetailScreen/BoardFootCalcScreen suman ~200 errores nuevos. El patrón es siempre el mismo: imports de react/react-native sin `@types/react` instalado + callbacks sin tipos explícitos.

### Aplicando los 5 fixes del Bug Fixer
Si Bug Fixer aplica los 5 fixes de mañana, estimación de reducción:
- Bug #1: -8 TS2741
- Bug #2: -7 TS2745
- Bug #3: -13 TS7006
- Bug #4: -4 (TS2591 + TS2307 cascada)
- Bug #5: -48 TS7006
- **Total estimado:** ~406 - 80 = **~326 errores** (base ~320 de infraestructura pura)

---

## 8. Novedades Positivas de Esta Noche

1. **8 nuevas pantallas paramétricas** — TableGenerator, CabinetGenerator, BenchGenerator, ShelfGenerator, DrawerCabinetGenerator, BoxGenerator, exportParametricPdf, ParametricHome. Funcionalidad real nueva.
2. **tools.json: 112 productos** (+5 vs ayer, nuevo máximo histórico).
3. **wood.json: 31 productos** (+3 vs ayer).
4. **Fix lazy load expo-print** — Previene crash `Cannot find native module 'ExpoPrint'` en Expo Go.
5. **CompareSheet nuevo** — Comparativa visual de herramientas implementada (24 errores pero lógica completa).
6. **0 botones rotos** — Ningún `onPress={() => {}}` vacío a pesar del volumen de código nuevo.

---

*Informe generado automáticamente por agente QA — DIY Project — 2026-04-17 05:00 Madrid*
