# BUG FIXER — 2026-04-16

**Agente:** bug-fixer automático (03:00 Madrid)
**Generado:** 2026-04-16T03:00
**Fuente de fixes:** PROPUESTAS_2026-04-15.md — sección "PROPUESTAS DE FIX PARA ERRORES CRÍTICOS"

---

## Baseline de errores TS (antes de fixes)

| Tipo | Cantidad | Descripción |
|------|---------|-------------|
| TS17004 | 411 | Cannot use JSX — `--jsx` flag no configurado |
| TS2307 | 121 | Cannot find module (react, react-native, etc.) |
| TS7006 | 48 | Parameter implicitly has 'any' type |
| TS6142 | 43 | Module resolved but `--jsx` not set |
| TS2304 | 10 | Cannot find name (\_\_dirname, \_\_DEV\_\_, process) |
| TS2591 | 9 | Cannot find name (require, process) |
| TS2322 | 9 | Type mismatch (excess property / key prop) |
| TS7053 | 7 | Element implicitly has 'any' type (indexing) |
| TS7031 | 4 | Binding element implicitly has 'any' type |
| TS6053 | 1 | File not found (expo/tsconfig.base) |
| **TOTAL** | **663** | — |

---

## Fixes Aplicados

### Fix #1 — `tsconfig.json` — Añadir jsx + lib + moduleResolution + exclude backend ✅ OK

**Correspondencia PROPUESTAS:** Fix #4 (backend/tsconfig.json) + Fix #3 (renderItem) — raíz real del problema

**Problema:** El `tsconfig.json` raíz extendía `expo/tsconfig.base` (no instalado), dejando el proyecto sin `jsx`, `lib`, ni `moduleResolution`. Esto causaba 411 errores TS17004 ("Cannot use JSX") y 43 TS6142. El backend quedaba compilado por el tsconfig raíz aunque tiene su propio tsconfig.

**Cambio ANTES:**
```json
{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  }
}
```

**Cambio DESPUÉS:**
```json
{
  "compilerOptions": {
    "strict": true,
    "jsx": "react-native",
    "lib": ["dom", "esnext"],
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "allowImportingTsExtensions": true,
    "noEmit": true
  },
  "exclude": ["backend", "node_modules"]
}
```

**Archivos:** `tsconfig.json`
**Delta errores:** 663 → 189 (**-474 errores**)
**Estado:** ✅ APLICADO — error count NO aumentó

---

### Fix #2 — `src/screens/ToolSearchScreen.tsx` — Tipar callbacks de SectionList ✅ OK

**Correspondencia PROPUESTAS:** Fix #3 (renderItem ToolSearchScreen)

**Problema:** Los callbacks `keyExtractor`, `renderSectionHeader` y `renderItem` del `SectionList` recibían `item`/`section` como `any` (SectionList de react-native no tipado en este entorno). Esto causaba TS7006, TS7031 y TS7053 al indexar `tierColors[item.tier]` y `tierLabels[item.tier]`.

**Cambios:**
```typescript
// ANTES
keyExtractor={item => item.id}
renderSectionHeader={({ section }) => (
renderItem={({ item }) => (

// DESPUÉS
keyExtractor={(item: ToolProduct) => item.id}
renderSectionHeader={({ section }: { section: { typeId: string; title: string; icon: string; data: ToolProduct[] } }) => (
renderItem={({ item }: { item: ToolProduct }) => (
```

**Archivos:** `src/screens/ToolSearchScreen.tsx` (líneas 85, 88, 95)
**Delta errores:** 189 → 183 (**-6 errores**)
**Estado:** ✅ APLICADO — error count NO aumentó

---

### Fix #3 — `src/screens/WoodCatalogScreen.tsx` — Tipar callbacks de FlatList ✅ OK

**Correspondencia PROPUESTAS:** Fix #3 (renderItem WoodCatalogScreen)

**Problema:** Los callbacks `keyExtractor` y `renderItem` del `FlatList` recibían `item` como `any`. Esto causaba TS7006, TS7031 y TS7053 al indexar `hardnessColors[item.hardness]`, `hardnessLabels[item.hardness]` y `useLabels[item.use]`.

**Cambios:**
```typescript
// ANTES
keyExtractor={item => item.id}
renderItem={({ item }) => (

// DESPUÉS
keyExtractor={(item: WoodProduct) => item.id}
renderItem={({ item }: { item: WoodProduct }) => (
```

**Archivos:** `src/screens/WoodCatalogScreen.tsx` (líneas 96, 98)
**Delta errores:** 183 → 173 (**-10 errores**)
**Estado:** ✅ APLICADO — error count NO aumentó

---

## Fixes No Aplicados

### Fix #1 PROPUESTAS — `DIYStepsScreen.tsx:27-28` — SKIP (ya aplicado)

El PROPUESTAS hacía referencia a `DIFFICULTY_LABELS`/`DIFFICULTY_COLORS` (names del código antiguo). El código actual ya usa `diffColors`/`diffLabels` con fallback correcto (commit anterior). **No aplica.**

### Fix #2 PROPUESTAS — `HomeScreen.tsx:133` — SKIP (cascade de react no instalado)

El error TS7006 en `p` (línea 131) y TS2322 en `ProjectCard` (línea 133) son cascadas de `useState` being `any` porque `react` no está instalado en el entorno CI. La fix real es `npm install`. Añadir `(p: Project)` no resuelve el TS2322 en `ProjectCard` ya que el componente requiere React types para JSX checking.

---

## Resumen Final

| Fix | Archivo | Estado | Delta errores |
|-----|---------|--------|---------------|
| #1 tsconfig jsx+lib+moduleResolution+exclude | `tsconfig.json` | ✅ OK | -474 |
| #2 renderItem ToolSearchScreen | `src/screens/ToolSearchScreen.tsx` | ✅ OK | -6 |
| #3 renderItem WoodCatalogScreen | `src/screens/WoodCatalogScreen.tsx` | ✅ OK | -10 |
| PROPUESTAS Fix#1 DIYStepsScreen | — | ⏭ SKIP (ya aplicado) | 0 |
| PROPUESTAS Fix#2 HomeScreen | — | ⏭ SKIP (cascade react) | 0 |

**Errores TS antes:** 663
**Errores TS después:** 173
**Delta total:** **-490 errores** (-73.9%)

**Archivos modificados:**
- `tsconfig.json`
- `src/screens/ToolSearchScreen.tsx`
- `src/screens/WoodCatalogScreen.tsx`

---

## Errores Restantes (173)

Los 173 errores restantes son principalmente:
- **TS2307** (121): `react`, `react-native`, `expo-*`, `@react-navigation/*` — módulos npm sin instalar. Fix: `npm install`.
- **TS7006/TS7031** (~30): Callbacks aún con `any` por dependencia de `react` no instalado (useState, useCallback).
- **TS2304** (~10): `__DEV__`, `__dirname`, `require` — globals de React Native / Node no declarados.
- **TS2322** (~9): Chips con `key` prop — desaparecerán con `@types/react` instalado.
- **TS7053** (~4): Indexaciones con `any` por cascada en pantallas aún no corregidas.

**Próxima acción recomendada:** `npm install` en entorno de desarrollo para resolver TS2307 de módulos externos.

---

*Generado automáticamente por agente bug-fixer — DIY Project — 2026-04-16T03:00*
