# BUG_FIXER — 2026-04-17

**Agente:** fix nocturno D0 (03:00 Madrid)
**Fuente:** PROPUESTAS_2026-04-15.md + errores actuales `npx tsc --noEmit`
**Baseline TS errors:** 421
**Errors tras fixes:** 406
**Delta:** -15 errores

---

## Nota previa

Los fixes #1–#5 descritos en PROPUESTAS_2026-04-15 ya estaban aplicados en el código:
- Fix #1 (DIYStepsScreen DIFFICULTY_LABELS): ya usa `diffLabels`/`diffColors` con fallback tipado.
- Fix #2 (HomeScreen Props `project`): `ProjectCard.Props` ya incluye `project: Project`.
- Fix #3 (renderItem tipado): ambos archivos ya tienen `({ item }: { item: ToolProduct/WoodProduct })`.
- Fix #4 (backend/tsconfig.json): ya tiene `lib: ["ES2020"]` y `types: ["node"]`.
- Fix #5 (Chip/FilterItem): patrón descrito no existe en ToolSearchScreen.

Se aplicaron los 3 fixes más impactantes sobre errores actuales reales.

---

## Fixes Aplicados OK

### Fix A — `src/screens/WoodCatalogScreen.tsx` — Chip key prop (4 errores)
**Problema:** El componente inline `Chip` no tenía `key` en su tipo de Props. Sin el módulo `react` resuelto, TypeScript trata `key` como propiedad excedente → TS2322 en las 4 llamadas a `<Chip key={...} />`.
**Cambio:** Línea 80 — añadido `key?: string | number` al tipo inline de Props.
```typescript
// ANTES
const Chip = ({ label, active, onPress }: { label: string; active: boolean; onPress: () => void }) => (
// DESPUÉS
const Chip = ({ label, active, onPress }: { label: string; active: boolean; onPress: () => void; key?: string | number }) => (
```
**Errores eliminados:** 4 (TS2322 en líneas 93, 99, 103, 107)
**Delta parcial:** 421 → 417

---

### Fix B — `src/screens/DIYStepsScreen.tsx` — Tipos explícitos en callbacks (7 errores)
**Problema:** `result` viene de `route.params` como `any`. Por cascada, `result.steps.map()` devuelve `any`, y `renderableSteps` queda sin tipo. Los callbacks en `useState` setter también son `any` porque `react` no está resuelto.
**Cambios:**
- `renderableSteps` anotado explícitamente como `ProjectStep[]`
- `(s: any)` en el `.map()` inicial (mapeo desde array no tipado)
- `(prev: ProjectStep[] | null)` y `(s: ProjectStep)` en los dos `setSteps()` callbacks
**Errores eliminados:** 7 (TS7006 en líneas 88, 95, 102, 104, 113, 115, 182)
**Delta parcial:** 417 → 410

---

### Fix C — `src/screens/ToolSearchScreen.tsx` — Tipos explícitos en callbacks + import ToolBrand (4 errores)
**Problema:** `productPool` y `brandsInPool` son `any` en cascada desde `useState` sin `react` resuelto. `keyExtractor` y `renderSectionHeader` también sin tipo.
**Cambios:**
- Importado `ToolBrand` desde `'../models/tools'`
- `productPool.map((p: ToolProduct) => ...)` — línea 113
- `TOOL_BRANDS.filter((b: ToolBrand) => ...)` — línea 114
- `brandsInPool.map((b: ToolBrand) => ...)` — línea 189
- `keyExtractor={(item: ToolProduct) => item.id}` — línea 232
- `renderSectionHeader={({ section }: { section: ToolSection }) => {` — línea 235
**Errores eliminados:** 4 (TS7006 en líneas 113, 189, 232; TS7031 en línea 235)
**Delta parcial:** 410 → 406

---

## Fixes FAILED

Ninguno. Los 3 fixes aplicados reducen errores sin introducir regresiones.

---

## Resumen

| Fix | Archivo | Errores eliminados | Estado |
|-----|---------|-------------------|--------|
| A | WoodCatalogScreen.tsx | 4 | ✅ OK |
| B | DIYStepsScreen.tsx | 7 | ✅ OK |
| C | ToolSearchScreen.tsx | 4 | ✅ OK |
| **Total** | 3 archivos | **15** | — |

**Errores TS baseline:** 421
**Errores TS final:** 406
**Delta total:** -15

---

## Archivos Modificados

- `src/screens/WoodCatalogScreen.tsx`
- `src/screens/DIYStepsScreen.tsx`
- `src/screens/ToolSearchScreen.tsx`

---

*Generado automáticamente por agente fix nocturno D0 — DIY Project — 2026-04-17T03:00*
